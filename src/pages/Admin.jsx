import CommonWrapper from "../components/CommonWrapper";

const Admin = () => {
  // const { data, setData } = useContext(CookiesContext);
  // console.log(JSON.parse(data));
  return (
    <>
      <CommonWrapper>
        <div>Admin</div>
        {/* <button
          onClick={() => setData("Sazzad Hosen Mahim")}
          className="bg-blue-500 p-5"
        >
          Update Cookies
        </button> */}
      </CommonWrapper>
    </>
  );
};

export default Admin;
